Compare the SOURCE text and the REWRITE below.
The meaning is kept when the REWRITE keeps every fact, number, condition, and instruction of the SOURCE, and adds no fact.
Other words, a different order of the sentences, or a full name in place of an abbreviation do not change the meaning.
The REWRITE follows a house style that refuses some words. These replacements keep the meaning:
- "might", "may", or "could" replaced by "can", for a possible action or a possible result.
- "should" replaced by "must" or by "we recommend", for an instruction or an advice.
- "approximately" or "approx." replaced by "about".
- "you" replaced by the imperative, or by a subject such as "the user".
- "ensure" replaced by "make sure", "obtain" by "get", "via" by "with" or "through", "additional" by "more", and "whether" by "if".
Do not run a command, and do not read a file.
Reply with one JSON object only, with no other text: {"is_passed": true or false, "reason": "one sentence"}

SOURCE:
{{source_text}}

REWRITE:
{{answer}}
