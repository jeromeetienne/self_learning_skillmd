An AI coding agent loads the skill `python-docstring`, reads one Python function, and writes the docstring of that function.
Code checks eight house rules on each docstring: the form of the string, the first line, the sections that name the parameters, the return value and the errors, the length of a line, and one refused phrase. The failures below name the rules.
The score is the percentage of rule checks that pass.
